-- Q21: Display all employees ordered by salary.
        SELECT *
        FROM EMP
        ORDER BY SAL;
-- Q22: Display employee names ordered alphabetically.
        SELECT ENAME
        FROM EMP
        ORDER BY ENAME;
-- Q23: Display employees ordered by hire date.
        SELECT * 
        FROM EMP
        ORDER BY HIREDATE; 
-- Q24: Display employees ordered by salary in descending order.
        SELECT *
        FROM EMP
        ORDER BY SAL DESC;
-- Q25: Display employees ordered by department number.
        SELECT *
        FROM EMP
        ORDER BY DEPTNO;